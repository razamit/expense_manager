# Documentation

The `docs/` folder holds supporting project documentation and local reference material that should not live in the runtime source tree.

## Design references

The `docs/design-reference/` folder holds UI exploration assets and screen references for the product.

Keeping those assets under `docs/` keeps the repository root focused on runtime code and setup entry points.