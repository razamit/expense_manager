# Prisma database URL (SQLite by default)
# The setup scripts copy this file to .env automatically when .env is missing.
# Keep the default value for local development unless you want Prisma to target a different database.
DATABASE_URL="file:./dev.db"
